#pragma once

// Thin forwarding header: the pooled D3D11 texture implementation lives in
// D3D11Texture.h/cpp (D3D11TexturePool). Kept so the CMake source list and
// include sites read naturally.

#include "graphics/D3D11/D3D11Texture.h"
