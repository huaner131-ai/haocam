# Presets (Phase 7)

Preset persistence arrives in Phase 7. The portable JSON schema is already
proven by `assets/presets/soft_idol.json` (parsed by unit tests). Preset
files are user-facing documents stored under the settings directory and
import/export as plain JSON.
